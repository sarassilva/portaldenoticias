version: '3.9'

services:
   db:
     image: mysql:5.7
     volumes:
       - db_data:/var/lib/mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: db4ass
       MYSQL_DATABASE: wordpress
       MYSQL_USER: devjota
       MYSQL_PASSWORD: db_my_sql_pa4$$

   wordpress:
     depends_on:
       - db
     image: wordpress:latest
     ports:
       - "8080:80"
     restart: always
     environment:
       WORDPRESS_DB_HOST: db:3306
       WORDPRESS_DB_USER: portalJT
       WORDPRESS_DB_PASSWORD: passjotaword
volumes:
    db_data: